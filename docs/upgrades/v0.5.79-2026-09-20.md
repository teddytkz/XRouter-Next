# Sync v0.5.76 → v0.5.79 (2026-09-20)

Rentang upstream: `6d11ebc0` (v0.5.75, sync sebelumnya) → `8e15f0bd` (v0.5.79).
27 commit upstream, 21 di antaranya menghasilkan perubahan di fork ini.

- Fork sebelum: `6a16375e`
- Fork sesudah: `3b945603`
- Versi fork tidak diubah (tetap `0.1.1` — jalur versi sendiri)

## Ringkasan

| Kategori | Jumlah |
|---|---|
| Cherry-pick mulus | 17 |
| Cherry-pick dengan konflik | 8 |
| Commit manual (bagian non-overlap dari konflik) | 3 |
| Commit test-fix (utang test upstream) | 1 |
| Commit terakhir | 21 |

## Mapping commit

### Cherry-pick mulus (17)

| Fork | Upstream | Subjek |
|---|---|---|
| `9e8db047` | `5c399b6` | feat(codebuddy-intl,ollama): add DeepSeek-V4.1-Flash |
| `e7db5cf2` | `13b468b` | fix(commandcode): preserve images and reasoning_effort on /alpha/generate |
| `185f3590` | `912ed29` | fix(deepseek,model-catalog): vision for V4.1-Flash ids, scope synced catalog |
| `e158b9eb` | `f4f06f2` | fix(translator): keep tool-result images, restore Kiro tool names |
| `6fec1cba` | `82b1bca` | fix(kiro): use neutral placeholder for tool-result-only user turns |
| `86e1d201` | `c49efdf` | fix(kiro): preserve underscores in tool names |
| `9b6e39bd` | `20a43f5` | fix(auth): don't cool down an account for a request-scoped 4xx |
| `b2373d36` | `367fc54` | feat(models): deepseek-v4.* accepts low..max effort |
| `c38a840f` | `725e2c1` | feat(i18n): integrate Persian (fa) translation |
| `d176811d` | `ef18175` | fix(zed): harden OAuth lifecycle and live model support |
| `ec904eb7` | `3ac100d` | fix(usage): improve DeepSeek credit balance display |
| `f818a1ef` | `f642295` | fix(antigravity): sanitize Hermes system identity |
| `c4144d21` | `4641c2b` | fix(zed): lower display priority in oauth list |
| `4b532d59` | `efc80ba` | fix(codex): route bare codex-auto-review to the Codex provider |
| `6b42f2a5` | `b3d6e08` | fix(antigravity): strip Claude Code billing header from system prompts |
| `cfc6fcae` | `092c84e` | fix(commandcode): retry on transient stream error and avoid fake stop chunks |
| `cc986d5f` | `bc3be0c` | fix(antigravity): scope cached thought signatures to the model family |

### Konflik — sudah ada di fork, TIDAK diambil

Delapan commit berikut semuanya berkonflik. Setelah diperiksa satu per satu,
**seluruhnya sudah ada di fork** — biasanya dengan implementasi fork yang lebih
baik. Tidak ada yang di-cherry-pick utuh.

| Upstream | Subjek | Kenapa tidak diambil |
|---|---|---|
| `058ceace` | fix(opencode): declare forceStream on transport | `registry/opencode.js` fork sudah punya `forceStream: true` **plus** komentar penjelasan gate-nya |
| `aa14ef72` | fix(opencode): normalize Muse Free tool choice | Fork sudah punya di `executor.transformRequest` (komentar PR #4062) |
| `2b65c49f` | fix(opencode): route Union Alpha through Messages API | Fork sudah punya `MESSAGES_MODELS` + `isMessagesModel` + header `anthropic-version` (PR #4099) |
| `702b57c3` | fix(opencode-go): route every responses-only model to /responses | Fork sudah punya, dan **lebih lengkap** — sekaligus strip suffix `model(level)` (PR #4084) |
| `eafac37d` | fix(opencode): strip prior reasoning items on Muse Spark Responses | Fork sudah punya di `sanitizeResponsesItems` |
| `6091ff59` | fix(opencode): resolve 403 FreeTierError with canonical session | Fork sudah punya: `OPENCODE_SESSION_RE`, `translateSessionId`, `generateSessionId` kanonik, UA `opencode/1.18.31` |
| `0c6ab4f9` | fix(opencode): reuse one stable upstream session per identity | Fork memakai `utils/sessionManager.js` **generik** (TTL + cleanup + semua provider) — lebih umum daripada map per-executor upstream. Bagian request-id-nya diambil manual (lihat bawah) |
| `93837af0` | fix(opencode): fix free tier 403 + China region | Decoy tools-nya **digantikan** `FINGERPRINT_TOOL_NAMES` fork. Tiga bagian non-overlap diambil manual (lihat bawah) |

> **Penting untuk sync berikutnya:** area opencode adalah tempat upstream paling
> sering bertabrakan dengan fork. Fork punya dua penggantian sadar:
>
> 1. **Fingerprint tool stubs** (`FINGERPRINT_TOOL_NAMES = ["bash","read"]` +
>    `ensureFingerprintTools`) menggantikan `cloakOpencodeTools` /
>    `OPENCODE_DECOY_*_TOOLS` upstream. Stub fork tidak dipanggil model dan
>    bentuknya disesuaikan per-endpoint (`/responses` flat vs chat nested).
> 2. **`utils/sessionManager.js` generik** menggantikan `stableOpencodeSessions`
>    (Map per-executor + `MEMORY_CONFIG`) upstream.
>
> Jangan timpa keduanya dengan versi upstream.

### Commit manual — bagian non-overlap yang diambil

| Fork | Sumber upstream | Isi |
|---|---|---|
| `e428e29c` | `0c6ab4f9` + `93837af0` | `x-opencode-request` deterministik (`deriveRequestId` dari session + pesan user terakhir, supaya retry berbagi id seperti CLI asli); case test koneksi `opencode` di `testUtils.js`; slice error 500/200 char agar link workspace China-region tidak terpotong; chip "Allow China-hosted models" di halaman provider |
| `3b945603` | `8e15f0bd` | DeepSeek-V4.1-Flash di registry `deepseek.js` + baris pricing (`deepseek-v4.1-flash`, `deepseek-flash`). Capabilities sudah ter-cover pola `*deepseek-v4.*` |
| `d370da78` | `f4f06f2` + `13b468b` | Menyelaraskan assertion test yang basi (lihat bawah) |

### Utang test upstream — diperbaiki di fork (`d370da78`)

`f4f06f29` dan `13b468b8` mengubah **source** tetapi tidak memperbarui test lama,
sehingga `upstream/master` **sendiri merah** di empat test ini. Empat lainnya
berubah karena dua `it.fails` (penanda bug) kini lulus setelah bug-nya diperbaiki.

| Test | Masalah | Perbaikan di fork |
|---|---|---|
| `unit/openai-to-commandcode.test.js` (2 test) | Source menambah field `mediaType`, test lama hanya mengharap `mimeType` | Tambah `mediaType` ke ekspektasi |
| `translator/bugs-gemini-cursor-commandcode.test.js` | Sama | Tambah `mediaType` ke ekspektasi |
| `unit/commandcode-to-openai.test.js` | Error event kini **throw**, bukan emit chunk `finish_reason:"stop"` | Ubah jadi `expect(...).toThrow(/Boom/)` |
| `translator/bugs-claudeCode-context.test.js` | `it.fails` — bug tool_result image sudah diperbaiki | Balik jadi `it` |
| `translator/bugs-openai-bridge.test.js` | Sama | Balik jadi `it` |

> Kalau sync berikutnya membawa test baru dari upstream yang menyentuh area ini,
> cek dulu apakah upstream sudah memperbaiki assertion-nya sendiri — kalau belum,
> fork akan tetap perlu penyesuaian yang sama.

## Verifikasi

- Gate regresi: **0 regresi baru**. 5 gagal tersisa (cline/kimi) sudah
  pre-existing sebelum cherry-pick — dibuktikan dengan `git stash`.
- Test stream + opencode: 76 lulus.
- ESLint: 3 error pre-existing di `providers/[id]/page.js` (baris 508/521/986 —
  `react-hooks/set-state-in-effect`, jauh dari edit sync ini).
- `verify-providers.mjs` mismatch = provider `freebuff` khas fork, pre-existing.

## Cara mengulang pemeriksaan

```bash
cd tests && npx vitest run unit/ translator/ --reporter=json \
  --outputFile=/tmp/xr.json --exclude='**/.kilo/**'
cd .. && node tests/__baseline__/verify-no-regression.mjs /tmp/xr.json
```
